import {build} from 'esbuild'
import {chromium} from 'playwright-core'
import postcss from 'postcss'
import tailwind from 'tailwindcss'
import assert from 'node:assert/strict'
const bundle=await build({stdin:{contents:`import React from 'react';import{createRoot}from'react-dom/client';import{MemoryRouter}from'react-router-dom';import Panel from './src/pages/HR/OvertimeManagement';createRoot(document.getElementById('root')).render(<MemoryRouter initialEntries={["/profile?tab=requests&request_type=overtime"]}><Panel reviewOnly/></MemoryRouter>);`,loader:'jsx',resolveDir:process.cwd()},bundle:true,jsx:'automatic',write:false,outdir:'out',plugins:[{name:'fixture',setup(b){b.onLoad({filter:/hrFoundation\.service\.js$/},()=>({contents:`export default {resolveEmployee:async()=>({id:'e1'}),getServiceRequests:async()=>[]}`,loader:'js'}));b.onLoad({filter:/hrCore\.service\.js$/},()=>({contents:`const row={id:'1',employee_name:'Test Employee',employee_code:'23022',work_date:'2026-09-10',requested_hours:'2.50',reason:'Delivery',status:'approved',approved_hours:'2.50',compensation_type:'cash',can_apply:true,stage_code:'hr_review',can_review:false,history:[],requested_by_name:'Manager'};window.actions=[];export default{applyOvertimeBenefit:async(id,data)=>{window.actions.push(data);return {...row,can_apply:false,application:{method:'cash',amount:'31.25',year:2026,month:9}}},getOvertimeDays:async()=>({days:[{date:'2026-09-11',overtime_hours:'3'}]}),getOvertimeEmployees:async()=>({employees:[{id:'e1',name:'Test Employee',code:'23022',is_self:true}]}),getOvertimeRequests:async(params)=>({count:params.search==='missing'?0:1,results:params.search==='missing'?[]:[row]}),createOvertimeRequest:async(data)=>{window.actions.push(data);return row},reviewOvertimeRequest:async(id,action,note)=>{window.actions.push(action);return {...row,status:'approved',can_review:false}}}`,loader:'js'}))}}]})
const css=await postcss([tailwind({content:['src/pages/HR/OvertimeManagement.jsx','src/components/HR/EmployeeServiceRequestsPanel.jsx']})]).process('@tailwind base;@tailwind components;@tailwind utilities;',{from:undefined})
const browser=await chromium.launch({channel:'chrome',headless:true})
try{const page=await browser.newPage({viewport:{width:1600,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.setContent('<div id="root" style="padding:24px"></div>');await page.addStyleTag({content:css.css+bundle.outputFiles.find(f=>f.path.endsWith('.css')).text});await page.addScriptTag({content:bundle.outputFiles.find(f=>f.path.endsWith('.js')).text});await page.getByRole('button',{name:'Test Employee',exact:true}).click();await page.getByRole('button',{name:'Apply',exact:true}).click();await page.getByLabel('Payroll month').fill('2026-09');await page.getByRole('button',{name:'Apply to monthly run',exact:true}).click();await page.getByRole('dialog').waitFor({state:'detached'});assert.equal(await page.evaluate(()=>actions[0].type),'cash');assert.equal(await page.evaluate(()=>actions[0].month),9);await page.getByText('Payroll 2026-09: AED 31.25',{exact:true}).waitFor();await page.setViewportSize({width:390,height:844});assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.getByLabel('Search overtime requests').fill('missing');await page.getByText('No overtime requests.',{exact:true}).waitFor();assert.deepEqual(errors,[]);console.log('Passed: OT benefit dialog, payroll period payload, applied detail and mobile layout.')}finally{await browser.close()}
